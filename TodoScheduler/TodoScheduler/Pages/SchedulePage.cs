using Xamarin.Forms;

namespace TodoScheduler.Pages
{
    public class SchedulePage : TodayPage
    {
        public SchedulePage()
        {
            /*
            ToolbarItem groupByTagItem = new ToolbarItem() {
                CommandParameter = "tag",
                Text = "Group by tag",
                Order = ToolbarItemOrder.Secondary
            };

            ToolbarItem groupByDate = new ToolbarItem() {
                CommandParameter = "date",
                Text = "Group by date",
                Order = ToolbarItemOrder.Secondary,
            };

            groupByDate.SetBinding(ToolbarItem.CommandProperty, new Binding("GroupCommand"));
            groupByTagItem.SetBinding(ToolbarItem.CommandProperty, new Binding("GroupCommand"));

            this.ToolbarItems.Add(groupByTagItem);
            this.ToolbarItems.Add(groupByDate);
            */
        }
    }
}
