using TodoScheduler.Controls;

namespace TodoScheduler.Pages
{
    public partial class TagDetailPage : BasePage
    {
        public TagDetailPage()
        {
            InitializeComponent();
            //list.ItemSelected += (s, e) => list.SelectedItem = null;
        }
    }
}
